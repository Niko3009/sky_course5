export const tokenFromStore = (store) => {
    return store.user.token
}

export const idFromStore = (store) => {
    return store.user.id
}

export const userFromStore = (store) => {
    return store.user
}
